# Use the official PHP image as a base image with Apache
FROM php:8.1-apache

# Install PDO and the MySQL driver
RUN docker-php-ext-install pdo pdo_mysql

# Enable Apache mod_rewrite (optional if you need URL rewriting)
RUN a2enmod rewrite

# Add a custom Apache configuration file (if needed)
COPY ./apache-config.conf /etc/apache2/sites-available/000-default.conf

# Copy the contents of the htdocs folder into the container's web root
COPY ./htdocs /var/www/html/

# Set the working directory to the web root
WORKDIR /var/www/html/

# Set correct permissions
RUN chown -R www-data:www-data /var/www/html

# Expose port 80
EXPOSE 80

# Start Apache server
CMD ["apache2-foreground"]
