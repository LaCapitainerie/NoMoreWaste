import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Panel | Incident",
  description: "Panel Incident",
};

export default function DemoLayout({children}: {children: React.ReactNode}) {
  return (
      children
  );
}